{
  "name": "@nearnight/readiness-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --import tsx --test ../../tests/readiness-gateway-contract.test.mjs",
    "typecheck": "tsc --noEmit --incremental false -p ../../tsconfig.json"
  }
}
