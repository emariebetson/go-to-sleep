export type OperationalArtifact<T>={version:1;releaseId:string;schema:string;result:T};
export function operationalArtifact<T>(result:T,environment:Record<string,string|undefined>=process.env){const releaseId=environment.RELEASE_ID,schema=environment.SCHEMA_CHECKSUM;if(!releaseId||!/^rel_[A-Za-z0-9_-]{8,100}$/.test(releaseId)||!schema||!/^[a-f0-9]{64}$/.test(schema))throw new Error("operational artifact identity invalid");return Object.freeze({version:1 as const,releaseId,schema,result})}
export function readOperationalArtifact<T>(value:unknown,releaseId:string,schema:string){const artifact=value as OperationalArtifact<T>;if(!artifact||Object.getPrototypeOf(artifact)!==Object.prototype||JSON.stringify(Object.keys(artifact).sort())!==JSON.stringify(["releaseId","result","schema","version"])||artifact.version!==1||artifact.releaseId!==releaseId||artifact.schema!==schema)throw new Error("operational artifact identity invalid");return artifact.result}
