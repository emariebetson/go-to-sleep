const USER=/^[A-Za-z0-9_.@-]{3,200}$/,PRINCIPAL=/^service:[A-Za-z0-9_-]{3,100}$/;
export type AdminPg={transaction<T>(run:(tx:{query<T>(sql:string,args:unknown[]):Promise<{rows:T[]}>})=>Promise<T>):Promise<T>};
export async function registerRolloutController(pg:AdminPg,databaseUser:string,principal:string){if(!USER.test(databaseUser)||!PRINCIPAL.test(principal))throw new Error("controller registration invalid");return pg.transaction(async tx=>{await tx.query(`GRANT nearyou_rollout_controller TO "${databaseUser.replaceAll('"','""')}"`,[]);await tx.query("SELECT nearyou.register_rollout_controller_identity($1::name,$2)",[databaseUser,principal]);const verified=(await tx.query<{ok:boolean}>("SELECT pg_has_role($1,'nearyou_rollout_controller','MEMBER') AND EXISTS(SELECT 1 FROM nearyou.rollout_controller_identities WHERE database_user=$1::name AND principal=$2) AS ok",[databaseUser,principal])).rows[0]?.ok;if(!verified)throw new Error("controller registration failed");return{controllerMappingVerified:true as const,artifact:{databaseUser,principal}}})}
