import{readFile}from"node:fs/promises";
export async function checkCatalogManifest(){const manifest=JSON.parse(await readFile(new URL("../postgres/catalog-manifest.json",import.meta.url),"utf8"))as Record<string,unknown>,migration=await readFile(new URL("../postgres/migrations/0004_product_readiness_evidence.sql",import.meta.url),"utf8");if(manifest.version!==1||manifest.schema!=="nearyou"||manifest.migrationHead!=="0004_product_readiness_evidence"||!Array.isArray(manifest.requiredKinds)||!manifest.requiredKinds.includes("membership")||!migration.includes("FORCE ROW LEVEL SECURITY")&&!(manifest.requireForcedRls as string[]).length)throw new Error("catalog manifest drift");return manifest}
if(import.meta.url===`file://${process.argv[1]}`)checkCatalogManifest().catch(()=>{process.exitCode=1});
