/* eslint-disable @typescript-eslint/no-explicit-any -- runtime pg module is optional */
import{writeFile}from"node:fs/promises";import{createHash}from"node:crypto";import{collectLiveCatalog}from"./postgres-catalog";
export async function generateCatalogCandidate(input:{databaseUrl:string;output:string;connect?:(url:string)=>Promise<any>}){if(!/^postgres(?:ql)?:\/\//.test(input.databaseUrl)||!input.output.endsWith("catalog-manifest.candidate.json"))throw new Error("catalog candidate invalid");const connect=input.connect??(async url=>{const name="pg",{Pool}=await import(name)as any,pool=new Pool({connectionString:url});return{query:(sql:string,args?:unknown[])=>pool.query(sql,args),close:()=>pool.end()}}),pg=await connect(input.databaseUrl);try{const rows=await collectLiveCatalog(pg),canonical=JSON.stringify(rows),catalogChecksum=createHash("sha256").update(canonical).digest("hex"),artifact={version:1,reviewRequired:true,generatedFrom:"supported-postgresql-16",catalogChecksum,rows};await writeFile(input.output,JSON.stringify(artifact,null,2)+"\n",{flag:"wx"});return artifact}finally{await pg.close()}}
if(import.meta.url===`file://${process.argv[1]}`){const output=process.argv[2],url=process.env.READINESS_CONTROL_DATABASE_URL;if(!url||!output)throw new Error("catalog candidate configuration missing");generateCatalogCandidate({databaseUrl:url,output}).catch(()=>{process.stderr.write("catalog candidate failed\n");process.exitCode=1})}
