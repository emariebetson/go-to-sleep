import type { AdminPg } from "./register-rollout-controller";
const USER=/^[A-Za-z0-9_.@-]{3,200}$/,PRINCIPAL=/^service:[A-Za-z0-9_-]{3,100}$/;
export async function registerPrivateTesterBaselineVerifier(pg:AdminPg,databaseUser:string,principal:string){if(!USER.test(databaseUser)||!PRINCIPAL.test(principal))throw new Error("baseline verifier registration invalid");return pg.transaction(async tx=>{await tx.query(`GRANT nearyou_private_tester_baseline_verifier TO "${databaseUser.replaceAll('"','""')}"`,[]);await tx.query("SELECT nearyou.register_private_tester_baseline_verifier_identity($1::name,$2)",[databaseUser,principal]);const verified=(await tx.query<{ok:boolean}>("SELECT pg_has_role($1,'nearyou_private_tester_baseline_verifier','MEMBER') AND EXISTS(SELECT 1 FROM nearyou.private_tester_baseline_verifier_identities WHERE database_user=$1::name AND principal=$2) AS ok",[databaseUser,principal])).rows[0]?.ok;if(!verified)throw new Error("baseline verifier registration failed");return{baselineVerifierMappingVerified:true as const,artifact:{databaseUser,principal}}})}
